Computer Network Final Project
team 13

1. Server的code在server/src裡，在server/中有Makefile，make all可以編譯server相關檔案，
make run可以將server跑起來
2. Client的code在client/src裡，在client/中有Makefile，make all可以編譯client相關檔案，
make run可以將client跑起來
3. 傳輸檔案時，client所在的位置需要有file資料夾，server也是，所以作業中有client/file和
server/file兩資料夾

